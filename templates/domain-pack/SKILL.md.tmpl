---
name: {{PACK_ID}}
description: {{TRIGGER_DESCRIPTION}}
---

# {{DISPLAY_NAME}}

## 工作边界

处理中国大陆法域内属于本业务领域的专业任务。识别任务不属于本领域时，说明边界并停止加载本包的专业模块。

## 建立任务上下文

在选择执行路径前，确认已有材料能够支持以下判断：

1. 使用者角色与代理立场；
2. 当前业务阶段与程序状态；
3. 具体任务目标与预期交付成果；
4. 已提供材料、缺失材料与相互冲突的事实；
5. 是否需要现行法规、案例或专业知识验证。

只询问会改变执行路径或最终成果的缺失信息。能够从材料中确定的事实不得重复询问。

## 选择执行路径

读取 [任务路由表](references/ROUTING.md)，然后只加载当前路径直接列出的工作流、场景知识、组合方案和质量标准。不要预先加载整个领域包。

## 使用 Lawyeah 能力

读取 `pack.json` 中当前路径声明的 MCP 能力级别：

- `required`：工具不可用时，不形成依赖该能力的最终专业结论；
- `recommended`：继续完成任务，并明确哪些判断未获得工具增强；
- `optional`：工具仅用于补充，不影响基本成果交付。

不要要求用户在对话中提供 ID、密钥或访问令牌。工具不可用时，继续完成不依赖外部依据的事实整理、问题识别和工作框架，并逐项标出受影响的判断节点。

## 形成成果

严格按照所选组合方案的输出合同生成成果。区分已确认事实、待确认事实、专业判断、依据状态和后续行动。不得把宽泛法律分析当作可交付成果。

## 质量检查

在交付前执行当前路径指定的质量标准，至少检查：

- 是否遗漏不利事实、相互冲突的材料或关键程序状态；
- 每个主要判断是否能够回到事实、证据和依据状态；
- 工具不可用时是否避免声称已经完成依据验证；
- 输出是否符合用户角色、代理立场和预期成果格式；
- 是否只加载并使用本领域内已经声明的专业模块。
