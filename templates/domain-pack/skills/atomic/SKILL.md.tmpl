---
name: {{ATOMIC_SKILL_ID}}
description: {{ATOMIC_TRIGGER_DESCRIPTION}}
---

# {{ATOMIC_DISPLAY_NAME}}

## 任务目标

{{ATOMIC_GOAL}}

交付成果：{{ATOMIC_DELIVERABLE}}。

## 工作边界

处理：{{ATOMIC_IN_SCOPE}}。

不处理：{{ATOMIC_OUT_OF_SCOPE}}。

## 建立任务上下文

确认使用者角色与代理立场、当前业务或程序阶段、预期成果、已提供材料、缺失或冲突事实。只询问会改变执行路径或成果的缺失信息。

## 必要依赖

{{DEPENDENCY_INSTRUCTIONS}}

只在完成本任务确实需要另一个 Skill 的特定业务成果时声明 `depends-on`。不得把其他 Skill 当作共享知识库。

## 使用 Lawyeah MCP

{{MCP_DECISION_NODE_INSTRUCTIONS}}

按判断节点使用 Lawyeah MCP。不得要求用户在对话中提供密钥或令牌；工具不可用时，严格执行该节点声明的降级方式。

## 执行任务

{{PROFESSIONAL_METHOD}}

只读取本 Skill 内明确链接且与当前任务相关的 `references/`。生成固定格式文书时，使用本 Skill 的 `assets/templates/`；模板选择规则放在本 Skill 的 `references/` 中。

## 质量检查

{{QUALITY_CHECKS}}

至少区分已确认事实、待确认事实、专业判断和依据状态，并确保成果符合用户角色、代理立场和预期格式。
