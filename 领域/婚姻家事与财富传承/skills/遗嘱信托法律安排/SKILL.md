---
name: 遗嘱信托法律安排
description: 为明确家庭目的形成遗嘱信托的财产、受托人、受益、治理、终止和执行法律文书方案。
---

# 遗嘱信托法律安排

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。


## 任务目标

为明确家庭目的形成遗嘱信托的财产、受托人、受益、治理、终止和执行法律文书方案。

交付：遗嘱信托法律结构表；遗嘱与信托条款草案。`depends-on` 为空。接受用户、律师、机关或其他来源的等价事实、测算和证据清单。

## 工作边界

正触发：设立人拟以遗嘱方式建立持续财产管理和受益安排。最低证据：目的、财产权属、受托受益主体、期限治理和监管边界。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 承诺金融机构承接、税务结果或投资收益
- 最近邻排除：只需一次性遗嘱处分而无持续管理
- 一般民商事合同审查起草转介审查起草领域（本窗搁置，只说明不实施）。C 端 109 大众专项只分流、不回改。刑事控告或辩护、税务测算、公司治理、投资金融产品、境外法或跨境执行只记录对本成果的影响并转介，不补造相邻领域结论。

本任务不给胜诉率。未知金额不写零。

## 建立评估坐标

确认：使用者与授权；单方立场；相对方；法域；当前协商、登记、诉讼或执行状态；机关文书作出与送达；关键材料及缺失。同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 材料表面；`A#` 当前一致／承认；`R#` 机关或生效文书；`C#` 约定或登记条款定位；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`Q#` 请求／抗辩；`V#` 评估版本；`E#` 发送／送达／履行凭证。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。不得输出胜诉率、必然受理、必然无效或唯一必得金额。未知金额不写零。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验中国大陆现行家事与程序规则；`search_practiced` 补路径与反例；有明确筛选条件时可用 `search_case_by_filter`；特殊事实比对可用 `search_case_by_fact`。文书结构可借鉴范本栏目。不虚构案件系统。

工具不可用时继续事实、主体、程序文书状态和问题清单；法律结论、期限或机关路径字段停止。

## 五个判断节点

### 判断节点 1　持续管理闸

执行顺序和停止条件以执行方法为准。这里只列入口，不代替节点。

### 判断节点 2　信托要素

执行顺序和停止条件以执行方法为准。这里只列入口，不代替节点。

### 判断节点 3　家庭法边界

执行顺序和停止条件以执行方法为准。这里只列入口，不代替节点。

### 判断节点 4　治理终止与执行

执行顺序和停止条件以执行方法为准。这里只列入口，不代替节点。

### 判断节点 5　文书边界

执行顺序和停止条件以执行方法为准。这里只列入口，不代替节点。


## 停止与安全

- `整体停止`：立场或授权不明；利益冲突未解；基础关系无法建立；用户坚持伪造、隐匿、威胁或违法取证。
- `字段停止`：法域、日期、文书送达、动态规则未核验。
- `目标停止`：主目标已变为相邻原子或本窗搁置的审查起草。
- 承诺承接或收益
- 一次性遗嘱即可
- 逃债或规避必留份

## 交付

严格按输出合同交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见路由。
