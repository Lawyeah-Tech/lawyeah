---
name: 准备行政诉讼庭审
description: 准备行政诉讼庭审争点、举证质证和代理提纲
---

# 准备行政诉讼庭审

## 任务目标

在明确、无利益冲突的单方代理立场下，形成争点、举证质证、询问和代理提纲。

`depends-on` 为空。接受用户、律师、机关或其他来源的等价事实、测算和证据清单。

## 工作边界

正触发：已开庭或即将开庭，需要庭审代理提纲。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 代替立案；
- 只要二审答辩；
- 只要阅卷目录；

覆盖复议共同被告庭审。不代替立案。

## 建立庭审坐标

确认：使用者与授权；案号；开庭传票；原告请求；被告和共同被告；已交换证据；是否赔偿一并请求。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 材料表面；`A#` 当前一致／承认；`R#` 裁判前文书；`C#` 规范定位；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`T#` 提纲项；`V#` 版本。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。不得把 1989 年行政诉讼法、1996 年行政处罚法或 1999／2017 年行政复议法当作现行文本。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验庭审程序、共同被告举证和非法证据排除；`search_practiced` 补庭审争点。

工具不可用时继续传票、请求和证据交换清单登记；依赖未核验规则的法律结论、期限或机关路径字段停止。

## 五个判断节点

执行顺序和停止条件以 [庭审准备方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1 争点清单

主体、事实、依据、程序、裁量、赔偿分列。
### 节点 2 共同被告

原行为合法性共同举证；复议决定合法性由复议机关承担。
### 节点 3 举证质证顺序

被告先就合法性举证是原则。提纲按此组织。
### 节点 4 询问

对执法人员、证人、鉴定人的问题清单。禁止诱导作伪。
### 节点 5 代理意见骨架

不是判决书。不给胜诉率。

## 停止与安全

- `整体停止`：指导伪证或威胁对方参加人。
- `字段停止`：庭审程序或举证责任规则未核验。
- `目标停止`：主目标已变为立案或二审文书。

## 交付

严格按 [输出合同](references/output-contract.md) 10节交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
