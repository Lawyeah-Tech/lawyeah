---
name: 办理协调方案执行力补强
description: 比较调解书、复议调解或司法确认等执行力补强路径
---

# 办理协调方案执行力补强

## 任务目标

在明确、无利益冲突的单方立场下，形成路径比较、材料和失败分流。

`depends-on` 为空。接受用户、律师、机关或其他来源的等价事实、测算和证据清单。

## 工作边界

正触发：需要通过调解书、复议调解或司法确认补强协调方案的执行力。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 把单独谅解写成已有执行力；
- 只要协议正文；
- 执行和解（执行阶段另项）；

单独谅解无执行力必须明示。

## 建立执行力补强坐标

确认：使用者与授权；已签署或拟签署文本；是否已立案或复议；对方是否同意由法院或复议机关确认。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 材料表面；`A#` 当前一致／承认；`R#` 文本；`C#` 规范定位；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`P#` 路径；`V#` 版本。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。不得把 1989 年行政诉讼法、1996 年行政处罚法或 1999／2017 年行政复议法当作现行文本。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验调解书效力、复议调解和司法确认相关规则。

工具不可用时继续文本状态和程序阶段登记；依赖未核验规则的法律结论、期限或机关路径字段停止。

## 五个判断节点

执行顺序和停止条件以 [执行力补强方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1 现状诊断

仅谅解、复议调解、诉讼调解书、司法确认候选分列。
### 节点 2 路径比较

时间、同意要件、可执行内容、失败后果。
### 节点 3 材料

身份、文本、授权、案件索引。
### 节点 4 失败分流

对方不配合则恢复复议诉讼或普通债权路径。
### 节点 5 明示

单独谅解无执行力必须写在成果封面。

## 停止与安全

- `整体停止`：把私下谅解包装成可立即强制执行的名义。
- `字段停止`：司法确认或复议调解效力未核验。
- `目标停止`：主目标已变为协议正文或执行和解。

## 交付

严格按 [输出合同](references/output-contract.md) 10节交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
