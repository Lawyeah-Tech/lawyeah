---
name: 提起行政诉讼上诉
description: 起草未生效行政裁判的上诉状、范围和期限情景
---

# 提起行政诉讼上诉

## 任务目标

在明确、无利益冲突的上诉方立场下，形成上诉状、范围、期限和理由。

`depends-on` 为空。接受用户、律师、机关或其他来源的等价事实、测算和证据清单。

## 工作边界

正触发：不服未生效行政裁判，要提起上诉。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 重做一审起诉；
- 再审（P2，unavailable）；
- 只要二审答辩；

不重做一审。判决与裁定上诉期限不同。

## 建立上诉坐标

确认：使用者与授权；一审案号和裁判种类；送达日；上诉请求范围；新证据有无。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 材料表面；`A#` 当前一致／承认；`R#` 一审裁判；`C#` 规范定位；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`Q#` 上诉请求；`K#` 期限；`V#` 稿件版本；`E#` 送达凭证。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。不得把 1989 年行政诉讼法、1996 年行政处罚法或 1999／2017 年行政复议法当作现行文本。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验行政诉讼法上诉期限和二审范围。

工具不可用时继续裁判种类、送达日和上诉请求登记；依赖未核验规则的法律结论、期限或机关路径字段停止。

## 五个判断节点

执行顺序和停止条件以 [上诉方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1 对象与期限

判决与裁定分列期限。送达不明则截止日停止。
### 节点 2 范围

对哪些判项不服。未上诉部分可能生效。
### 节点 3 理由

事实、法律、程序。不把一审起诉状再贴一遍。
### 节点 4 新证据

二审新证据条件核验。不得突击非法证据。
### 节点 5 草稿

上诉状草案。不代提交。

## 停止与安全

- `整体停止`：裁判已生效仍制作普通上诉状冒充未生效。
- `字段停止`：上诉期限未核验；送达不明。
- `目标停止`：主目标已变为再审或二审答辩。

## 交付

严格按 [输出合同](references/output-contract.md) 10节交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
