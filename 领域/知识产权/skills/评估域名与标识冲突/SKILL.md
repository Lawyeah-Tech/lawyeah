---
name: 评估域名与标识冲突
description: 评估域名抢注投诉与诉讼分流，不代提交仲裁
---

# 评估域名与标识冲突

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。不得把 1990 年《著作权法》同号旧义或 1993 年《反不正当竞争法》定义当作现行文本。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 任务目标

在明确、无利益冲突的单方立场下，形成“域名争议路径评估”：域名争议路径、与商标或仿冒分流、投诉或诉讼接口。不代提交域名仲裁。纯注册服务咨询不收录。

`depends-on` 为空。接受用户、律师、机关或其他来源的等价事实、评估、测算和证据清单。

## 工作边界

正触发：域名抢注、投诉或标识与域名冲突，需要路径选择，而不是要代提交投诉或纯注册手续。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 只要代提交域名仲裁或投诉；
- 纯域名注册续费手续咨询；
- 只要注册商标近似对照且域名只是背景；
- 主轴是劳动、数据合规或反垄断；

本任务不代提交域名仲裁，不保证转移域名，不把持有域名写成当然侵权。

## 建立工作坐标

确认：使用者与授权；单方立场；相对方；权利类型与对象；法域；当前通知、平台、行政、诉讼或执行状态；关键材料及缺失。域名注册时间、标识权利时间、使用网站内容、争议政策文本（CNNIC 等）。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。口头转述和“已读”不写成已受理、已送达或期限已起算。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 材料表面；`A#` 当前一致／承认；`R#` 机关或生效文书；`C#` 权利文件或条款定位；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`Q#` 请求／抗辩；`V#` 版本；`E#` 发送／送达／使用凭证。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。不得输出胜诉率。未知金额不写零。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验域名争议政策与反法第 7 条、商标法接口。

MCP 把 2025-10-15 已施行的《反不正当竞争法》标成尚未施行时，以施行日为准并记冲突。旧条号（专利法第六十五条、著作权法第四十九条、反法第九条当现行定义）记噪声。search_law 返回已失效《合同法》时不得写入 L#。

工具不可用时继续事实、对象和问题清单；法律结论、期限或机关路径字段停止。

## 五个判断节点

执行顺序和停止条件以 [方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　路径

域名争议解决、商标民事、仿冒、行政诉讼（如有）分列。

### 节点 2　投诉要件

相同或混淆性相似、无权利或合法利益、恶意。三要件未核验政策文本则字段停止。

### 节点 3　与商标分流

在核定商品上的商标侵权对照另转。

### 节点 4　恶意

要约高价、屏蔽权利人、假冒网站可作迹象，不得写成犯罪。

### 节点 5　提交

不代提交。过期或政策不适用则停止该路径。

## 停止与安全

- `整体停止`：立场或授权不明；利益冲突未解；基础主体或对象无法建立；用户坚持伪造、隐匿、威胁刑事或违法取证。
- `字段停止`：法域、日期、文书送达、动态规则未核验。
- `目标停止`：主目标变为代提交或纯注册手续。

## 交付

严格按 [输出合同](references/output-contract.md) 交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
