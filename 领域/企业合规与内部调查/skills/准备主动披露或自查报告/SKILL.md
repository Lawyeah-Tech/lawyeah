---
name: 准备主动披露或自查报告
description: 形成向机关提交前的自查披露稿和不可逆提示
---

# 准备主动披露或自查报告

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 任务目标

在事实材料和授权支持对外书面自查或披露时，形成发送前披露包：发送闸、披露范围、保留项、表述约束、到达设计和不可逆提示。不执行发送，不写成已获从宽。

`depends-on` 为空。接受用户、律师、内审、机关或其他来源的等价事实、授权、测算和证据清单，不要求先运行其他 Skill。

## 工作边界

正触发：公司拟向市场监管、国资监管、主管机关或行业监管提交自查/情况说明，且主成果是发送前稿。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 主成果是刑事自首、认罪认罚或辩护；
- 主成果是检察合规第三方验收包；
- 只要内部调查报告，不对外；
- 检查人员已在现场只要现场纪律；
- 事实明显不足仍要求宣称“问题已查清并整改完毕”；

本任务不给胜诉率、必然免责、刑事既遂或唯一必得总额。

## 建立坐标

确认：接收机关与文书依据；披露事实范围；已核验与未核验部分；是否并行刑事/监察；签发人；拟提交日。

同时掌握公司与被调查对象相互冲突的秘密时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` `D#` `A#` `R#` `C#` `L#` `I#` `G#` `V#` `E#`。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验自查从宽、主动交代、行政处罚从轻和国资报告义务；`search_template` 只借鉴栏目。

工具不可用时继续事实、主体和问题清单；依赖未核验规则的法律结论、期限或机关路径字段停止。

## 五个判断节点

执行顺序和停止条件以 [方法](references/method.md) 为准。这里只列入口，不代替节点。

### 1. 发送闸

没有可回链事实和签发授权不得打开。把猜测写成已查清：停止终稿。

### 2. 范围与保留

已核实、情景、无法估计分列。未知不写零。保留项须标明“尚未核验”，禁止用“全面合规”遮盖。

### 3. 表述约束

不承认未核验的犯罪构成。不承诺他人罪责。不保证“提交即免罚”。涉及个人犯罪：转介刑事，本任务只保留企业事实段。

### 4. 到达与版本控制

提交渠道、签收、副本编号。不得把快递发出写成机关已认定。

### 5. 与检察合规、检查应对的分界

已进入第三方评估机制转介检察合规原子。人员已在现场转介检查应对。本任务只做书面披露稿。

## 停止与安全

- `整体停止`：伪造情节、隐匿关键不利事实、顶替责任人。
- `字段停止`：接收机关权限、从宽规则、施行日未核验。
- `目标停止`：主目标变为刑事自首或验收包。

## 交付

严格按 [输出合同](references/output-contract.md) 十节交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
