---
name: 建立内部调查证据案卷
description: 形成要件、材料、合法取得与补证矩阵
---

# 建立内部调查证据案卷

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 任务目标

形成内部调查证据案卷矩阵：待证要件、支持与不利材料、控制方、合法取得、缺口、补证步骤和最晚时点。不是调查结论，不是诉状。

`depends-on` 为空。接受用户、律师、内审、机关或其他来源的等价事实、授权、测算和证据清单，不要求先运行其他 Skill。

## 工作边界

正触发：已有或准备内部调查，需要把材料做成可回链案卷。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 只要策略总图或是否启动；
- 只要调查报告结论；
- 要求提供入侵、秘密录音、购买黑客数据或隐匿方案；
- 主成果是劳动仲裁证据或刑事阅卷；

本任务不给胜诉率、必然免责、刑事既遂或唯一必得总额。

## 建立坐标

确认：调查范围；待证假设；已持有材料来源；对方或第三人控制的线索；是否已发持有通知。

同时掌握公司与被调查对象相互冲突的秘密时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` `D#` `EV#` `C#` `L#` `I#` `G#` `Q#` `V#`。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验非法证据排除、电子数据、会计档案和举报保密。

工具不可用时继续事实、主体和问题清单；依赖未核验规则的法律结论、期限或机关路径字段停止。

## 五个判断节点

执行顺序和停止条件以 [方法](references/method.md) 为准。这里只列入口，不代替节点。

### 1. 专项分流和待证要件

把每个假设拆成要件，不把回扣写成已构成犯罪。

### 2. 合法取得

记录形成主体、取得方式、权限依据。对方控制不得推定存在。

### 3. 支持与不利并列

每个要件必须同时列不利材料。不得只收有利件。

### 4. 缺口与补证

补证必须合法。不能合法取得的标停止。

### 5. 禁止动作

首次要求伪造、隐匿、偷拷、窃听、非法侵入：该部分整体停止。

## 停止与安全

- `整体停止`：授权不明或坚持违法取证。
- `字段停止`：举证责任或非法证据规则未核验。
- `目标停止`：主目标变为报告、诉状或违法方案。

## 交付

严格按 [输出合同](references/output-contract.md) 十节交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
