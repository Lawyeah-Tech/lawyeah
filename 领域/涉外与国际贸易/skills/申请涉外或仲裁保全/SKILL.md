---
name: 申请涉外或仲裁保全
description: 准备涉外诉讼或仲裁的财产、行为或证据保全申请材料和担保
---

# 申请涉外或仲裁保全

## 任务目标

在明确、无利益冲突的申请方立场下，形成保全申请材料、担保和管辖；不保证裁定；海事保全转海事。

`depends-on` 为空。接受用户、律师、机关或其他来源的等价事实、测算和证据清单。

## 工作边界

正触发：已决定申请财产、行为或证据保全，且主轴不是海事扣船。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 海事保全、扣船、海事证据保全；
- 不保证之外还要求预测裁定结果；
- 应对保全裁定为主；

不保证裁定。海事保全转海事。禁止伪造财产线索。

## 建立评估坐标

确认：已诉或拟诉、已仲裁或拟仲裁；保全种类；标的；财产线索来源；担保能力；管辖法院或仲裁机构；紧急情况。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 财产线索；`A#` 已立案或仲裁；`R#` 机构受理；`C#` 请求权基础；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`Q#` 保全项；`V#` 申请版本。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。未知金额不写零。

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验民事诉讼法保全、仲裁保全及涉外仲裁保全提交法院的接口。工具不可用时继续做财产线索编目；管辖和担保数额结论字段停止。

## 五个判断节点

执行顺序和停止条件以 [评估方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　海事与目标闸

扣船转海事。应对裁定转出。伪造线索停止。

### 节点 2　种类与紧迫

诉前裁前期限待核。自行公证转保全证据原子。

### 节点 3　管辖

仲裁保全法院连结待核。不足不选定唯一法院。

### 节点 4　范围与担保

金额未知不写零。担保不等于必然裁定。

### 节点 5　草稿

不代提交。提示申请错误赔偿风险。

## 停止与安全

- `整体停止`：伪造财产线索；海事扣船仍要本任务终稿。
- `字段停止`：保全管辖、仲裁保全路径、担保规则未核验。
- `目标停止`：主目标变为海事保全或应对裁定。

## 交付

严格按 [输出合同](references/output-contract.md) 交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
