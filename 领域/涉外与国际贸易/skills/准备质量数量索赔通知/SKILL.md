---
name: 准备质量数量索赔通知
description: 在检验窗口内固定质量或数量索赔意思及具体项目
---

# 准备质量数量索赔通知

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 任务目标

在明确、无利益冲突的主张方立场下，形成索赔通知、项目和期限；不替代精算终稿。

`depends-on` 为空。接受用户、律师、机关或其他来源的等价事实、测算和证据清单。

## 工作边界

正触发：检验期内需要固定质量或数量索赔意思，本次主成果是索赔通知而不是精算。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 精算终稿；
- 解除通知作为主成果；
- 检验期是否逾期仍是主争点且不要发函；

不替代精算终稿。检验期可能逾期时标红旗，通知不能补正已届满的不变期间。

## 建立评估坐标

确认：到货和发现不符日期；合同检验索赔条款；拟主张的短量或质量问题；已有检验材料；接收人；作准文本。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 检验记录；`A#` 到货事实；`R#` 商检；`C#` 索赔条款；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`Q#` 索赔项；`V#` 稿件版本。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。未知金额不写零。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验通知具体性和及时性。CISG 第 39 条未核验不得直接套用。工具不可用时继续回链事实；通知是否仍有效的结论字段停止。

## 五个判断节点

执行顺序和停止条件以 [评估方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　任务闸

期间判断、解除、精算、海事转出。伪造停止。

### 节点 2　窗口红旗

可能逾期不得声称完全按期。不变期间不能补正。

### 节点 3　具体性

须写批次现象标准。无鉴定不写已根本违约。

### 节点 4　请求边界

拒收接近解除则标红旗。金额未知不写零。

### 节点 5　送达与交付

已读不等于到达。文稿未发送。

## 停止与安全

- `整体停止`：伪造检验或倒签通知；把本稿写成解除。
- `字段停止`：通知具体性规则、检验期未核验。
- `目标停止`：主目标变为精算或解除通知。

## 交付

严格按 [输出合同](references/output-contract.md) 交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
