---
name: 评估框架协议与分订单冲突
description: 判断框架、PI、订单和信用证的效力层级及本约成立节点
---

# 评估框架协议与分订单冲突

## 任务目标

在明确、无利益冲突的单方立场下，形成文件效力层级和构成本约节点，不按单张订单另建 Skill。

`depends-on` 为空。接受用户、律师、机关或其他来源的等价事实、测算和证据清单。

## 工作边界

正触发：框架、形式发票、订单、信用证不一致，需要判断哪一层构成约束及冲突如何处理。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 只有一份合同且无文件冲突；
- 未成争要重写框架（审查起草搁置）；
- 按每一张订单要一个独立评估包作为新技能；

不按单张订单另建 Skill。多订单在同一层级表处理。

## 建立评估坐标

确认：框架协议及期限；PI；PO；确认函；信用证；修改记录；实际履行的订单号。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 各文件表面；`A#` 已履行订单；`R#` 机构对文件的认定；`C#` 冲突条款定位；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`Q#` 层级假设；`V#` 评估版本。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。未知金额不写零。

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验要约承诺、格式条款、合同成立。CISG 第 14–19 条未核验不得直接套用。工具不可用时继续做文件时间线；本约是否成立的结论字段停止。

## 五个判断节点

执行顺序和停止条件以 [评估方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　任务闸

无冲突转履行评估。伪造确认停止。

### 节点 2　文件时间线

过期报价被确认则承诺效力不足。

### 节点 3　框架与本约

意向数量通常不构成本约。仲裁条款可能独立约束。

### 节点 4　冲突处理

后合同优先只是待核方法。信用证与本约分账。

### 节点 5　下一步

输出已成立订单清单。不另建单张订单技能。

## 停止与安全

- `整体停止`：伪造订单确认；立场不明。
- `字段停止`：要约承诺、CISG 第 19 条、格式条款未核验。
- `目标停止`：主目标变为重写框架或只打信用证不符点。

## 交付

严格按 [输出合同](references/output-contract.md) 交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
