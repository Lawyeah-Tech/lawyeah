---
name: 准备外商投资信息报告材料
description: 准备外商投资初始、变更或年度信息报告材料和时点补正
---

# 准备外商投资信息报告材料

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 任务目标

在明确、无利益冲突的申报义务人立场下，形成外商投资信息报告材料、时点和补正，不替代安全审查申报终稿。

`depends-on` 为空。接受用户、律师、机关或其他来源的等价事实、测算和证据清单。

## 工作边界

正触发：已决定履行初始、变更或年度报告，本次主成果是信息报告材料。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 尚未判断要不要做安全审查；
- 把信息报告当成投资批准；
- 只要负面清单评估；

不替代安全审查申报终稿。信息报告不是批准证书。

## 建立评估坐标

确认：报告类型（初始／变更／年度）；企业与投资者；统一社会信用代码；变更事项；报告时点；市场监管和商务两套系统接口待核。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 登记或系统截图；`A#` 已确认股权；`R#` 市场监管登记；`C#` 章程或决议；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`Q#` 报告项；`V#` 材料版本。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。未知金额不写零。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验外商投资信息报告办法及现行公告。不得使用已废外资备案办法。工具不可用时继续按已知变更事实编目；法定时点字段停止。

## 五个判断节点

执行顺序和停止条件以 [评估方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　任务闸

安全审查和准入总评转出。伪造穿透停止。

### 节点 2　报告类型

初始、变更、年度分列。多报合一口径待核。

### 节点 3　时点

对照办法原文。不编造一律 30 日。

### 节点 4　材料与穿透

与登记一致。协议控制不得教唆隐瞒。

### 节点 5　与安全审查边界

报告不是批准。可能属审查范围则显著红旗。

## 停止与安全

- `整体停止`：伪造穿透或实际控制人；用报告冒充批准。
- `字段停止`：报告办法时点、变更范围未核验。
- `目标停止`：主目标变为安全审查终稿或交易文件。

## 交付

严格按 [输出合同](references/output-contract.md) 交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
