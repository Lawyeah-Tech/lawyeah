---
name: 应对出口管制问询或调查
description: 应对出口管制问询或调查的答复范围、补证和刑事红旗
---

# 应对出口管制问询或调查

## 任务目标

在明确、无利益冲突的被调查方立场下，形成答复范围、权利保留、补证和刑事红旗；不教唆隐匿；刑案转刑事。

`depends-on` 为空。接受用户、律师、机关或其他来源的等价事实、测算和证据清单。

## 工作边界

正触发：商务或海关已问询、约谈或立案调查出口管制事项，本次主成果是应对方案。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 尚未被问询只要合规评估；
- 刑案已立且主成果是辩护；
- 教唆隐匿或统一口径作伪；

不教唆隐匿。刑案转刑事。不预测调查结论。

## 建立评估坐标

确认：文书种类（问询函、约谈通知、立案告知）；作出机关；收到日；答复期限；调查范围；已提交材料；是否同步海关或公安。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 机关文书；`A#` 已承认出口事实；`R#` 问询或立案文书；`C#` 合同与许可；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`Q#` 答复项；`V#` 答复策略版本。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。未知金额不写零。

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验出口管制法监督检查、海关法稽查与行政处罚接口、与刑事移送的边界。期限以文书载明和现行法核验。工具不可用时继续固定文书和事实范围；期限和移送结论字段停止。

## 五个判断节点

执行顺序和停止条件以 [评估方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　文书与刑民闸

无文书回评估。刑案转刑事。毁证停止。

### 节点 2　范围与期限

不主动扩大。期限以文书为准，不编造日数。

### 节点 3　事实答复边界

未知用途不猜。法律定性可停止。

### 节点 4　补证与刑事红旗

伪报假用户标刑事红旗。不写讯问话术。

### 节点 5　答复稿边界

只出事实说明和目录。不代提交。

## 停止与安全

- `整体停止`：隐匿销毁证据；补假许可；刑案仍要本任务写辩护。
- `字段停止`：答复期限、移送标准未核验。
- `目标停止`：主目标变为刑辩或尚未被问询的评估。

## 交付

严格按 [输出合同](references/output-contract.md) 交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
