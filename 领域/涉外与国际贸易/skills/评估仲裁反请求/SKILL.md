---
name: 评估仲裁反请求
description: 评估涉外仲裁反请求的协议覆盖、期限和草案边界
---

# 评估仲裁反请求

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 任务目标

在明确、无利益冲突的被请求方立场下，形成反请求条件、期限和请求草案；不自动生成反请求终稿。

`depends-on` 为空。接受用户、律师、机关或其他来源的等价事实、测算和证据清单。

## 工作边界

正触发：被申请人拟在中国涉外仲裁中提反请求，需要条件与期限判断。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 自动生成反请求终稿且条件未闭合；
- 原告在诉讼中提反诉作为主成果；
- 申请方主请求终稿；

不自动生成反请求终稿。条件或期限未闭合只出草案和缺口。

## 建立评估坐标

确认：仲裁机构与规则版本；主请求；拟反请求；提出期限；是否同一法律关系或规则允许的范围；是否已过首次答辩。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 仲裁通知和规则；`A#` 主请求；`R#` 机构通知；`C#` 仲裁协议覆盖；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`Q#` 反请求；`V#` 草案版本。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。未知金额不写零。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验仲裁协议覆盖范围；机构规则以该机构现行文本为准，技能不得冒充已核验期限条文。工具或规则不可用时继续做请求对照；期限和可仲裁性字段停止。

## 五个判断节点

执行顺序和停止条件以 [评估方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　任务闸

诉讼反诉、主请求申请转出。伪造停止。

### 节点 2　协议覆盖

未覆盖不得塞进本案。担保侵权可能超出。

### 节点 3　期限

逾期不写成应当受理。

### 节点 4　牵连与草案

抵销与独立反请求分列。未知金额不写零。

### 节点 5　下一步

条件未闭合不升级终稿。提交走应诉接口。

## 停止与安全

- `整体停止`：伪造反请求；规则未核验仍要可提交终稿。
- `字段停止`：机构规则期限、协议覆盖未核验。
- `目标停止`：主目标变为诉讼反诉或主请求申请。

## 交付

严格按 [输出合同](references/output-contract.md) 交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
