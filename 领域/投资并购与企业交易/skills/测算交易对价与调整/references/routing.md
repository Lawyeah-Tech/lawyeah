# 边界与路由

## 正触发

需要对并购对价、净债务、营运资金、锁箱泄漏或交割账做分项数字。

## 负触发

| 主要目标 | 路由 |
| --- | --- |
| 对赌或回购效力 | 未安装则 `unavailable`，本任务只留数字接口 |
| 投后能否索赔 | `评估投后赔偿与或有对价请求` |
| 税负 | 税务领域 |
| 结构总图 | `评估投资并购交易结构` |

## 易混

条款有公式但参数不全时正触发，输出部分形成。用户要“就报一个数去谈”且拒绝列未知时停止唯一总额。
