# 边界与路由

## 正触发

终止权材料支持发送，需要发送前终止通知和到达设计。

## 负触发

| 主要目标 | 路由 |
| --- | --- |
| 只要判断能不能走 | 未安装评估原子则 `unavailable`，本任务不出终稿 |
| 一般合同解除 | 合同争议领域 |
| 投后赔偿通知 | 未安装则 `unavailable` |
| 交割包 | `准备交割文件包` |

## 易混

条件逾期或 MAC 事实存在但终止权未闭合时负触发。发送闸打开后正触发，状态保持待发送。
