---
name: 准备增资认购协议
description: 准备发送前增资额估值资金用途交割和章程接口
---

# 准备增资认购协议

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 任务目标

在明确、无利益冲突的单方立场下，形成“发送前增资认购协议”：增资额、估值、资金用途、交割和章程修改接口

`depends-on` 为空。接受用户、律师、财务顾问或其他来源的等价事实、估值、清单和文本。本任务不把未核验规则写成已闭合结论。

## 工作边界

正触发：以增资取得股权，需发送前认购协议；起草方。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 日常增资治理或登记本身；
- 只要完整 SHA；
- 老股转让路径却要认购协议；
- C 端小微内部增资填空；

本任务日常增资治理转公司治理；不出完整 SHA，不给成交率或胜诉率。

## 建立出稿坐标

确认：使用者与授权；单方立场；增资额与估值；新老股东；资金用途；是否连带对赌；法域；当前磋商、文本、审批或交割状态；关键材料及缺失。

同时掌握买卖双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 陈述；`D#` 估值／决议；`C#` TS／章程；`L#` 已核验规则；`I#` 判断；`Q#` 条款块；`V#` 版本

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验增资决议、出资和登记；不虚构审批系统、案号或登记回执。

工具不可用时继续按增资事实出受限草案，决议和登记字段停止；依赖未核验规则的法律结论、期限或机关路径字段停止。

## 五个判断节点

执行顺序和停止条件以 [起草方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　发送闸

老股路径转 SPA。日常无交易增资转公司治理。登记全包另项。

### 节点 2　增资与估值

认购额、计入注册资本与资本公积、投前投后估值分列。未知不写零。

### 节点 3　决议、优先认缴与用途

股东会决议、优先认缴放弃、资金用途和监管账户接口。

### 节点 4　交割与对赌接口

缴付、名册、登记。对赌义务主体是股东还是公司必须可见。

### 节点 5　与 SHA／章程

本任务只留接口，不出完整 SHA。章程修正另项可交叉。

## 停止与安全

- `整体停止`：立场冲突；抽逃或虚假出资安排；把对公司对赌写成当然有效。
- `字段停止`：决议、优先认缴、出资形式未核验。
- `目标停止`：主目标变为日常治理或完整 SHA。

## 交付

严格按 [输出合同](references/output-contract.md) 十节交付。正触发状态只用 `草案、待签署、停止`。边界见 [路由](references/routing.md)。
