---
name: 评估重大不利变化条件
description: 判断MAC范围例外举证责任和最强反方
---

# 评估重大不利变化条件

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 任务目标

在明确、无利益冲突的单方立场下，形成“重大不利变化条件评估”：MAC 范围、例外、举证责任、最强反方

`depends-on` 为空。接受用户、律师、财务顾问或其他来源的等价事实、估值、清单和文本。本任务不把未核验规则写成已闭合结论。

## 工作边界

正触发：拟用或被主张 MAC 作为不交割理由。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 发出终止通知；
- 只要交割条件总表；
- 宏观叙事而无条款和事实；
- 一般合同审查起草（本窗搁置）；

本任务不发出终止通知，不给成交率或胜诉率。

## 建立评估坐标

确认：使用者与授权；单方立场；MAC 条款文本；被主张事实；例外清单；距交割时间；法域；当前磋商、文本、审批或交割状态；关键材料及缺失。

同时掌握买卖双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 陈述；`D#` 事件材料；`C#` MAC 条款；`L#` 已核验规则；`I#` 判断；`Q#` 主张；`V#` 版本

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验情势变更、合同解释和举证责任；案例只比较差异不写必然；不虚构审批系统、案号或登记回执。

工具不可用时继续条款与事实对照，成立字段停止；依赖未核验规则的法律结论、期限或机关路径字段停止。

## 五个判断节点

执行顺序和停止条件以 [评估方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　分流

终止通知另项。交割条件总表另项。无条款的情势变更只标接口。

### 节点 2　范围与例外

目标自身、行业、宏观、疫情／政策例外分列。例外命中则难以支持。

### 节点 3　量化与持续

条款若要求重大、持续、对整体经营：事实必须可回链。不得用股价单独代替，除非条款如此。

### 节点 4　举证责任与最强反方

主张方通常承担。对方会主张例外、已知、已披露或不可量化。

### 节点 5　下一步

支持发出终止才转通知原子。否则转谈判或继续交割。

## 停止与安全

- `整体停止`：立场冲突；捏造 MAC 事实。
- `字段停止`：条款解释、情势变更未核验。
- `目标停止`：主目标变为发终止通知。

## 交付

严格按 [输出合同](references/output-contract.md) 十节交付。正触发状态只用 `已形成、部分形成并明确受限字段、因停止条件未形成`。边界见 [路由](references/routing.md)。
