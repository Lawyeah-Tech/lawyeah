---
name: 评估交割前后许可资质承继
description: 判断特许牌照能否随转及先批后交割红旗
---

# 评估交割前后许可资质承继

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 任务目标

在明确、无利益冲突的单方立场下，形成“许可资质承继评估表”：许可清单、可否承继、先批后交割红旗

`depends-on` 为空。接受用户、律师、财务顾问或其他来源的等价事实、估值、清单和文本。本任务不把未核验规则写成已闭合结论。

## 工作边界

正触发：交易依赖特许、牌照或行业许可能否随转。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 只要行业监管申报正文或代办许可；
- 环境许可、排污权、能源监管专项尽调；
- 只要完整法律尽调报告；
- 一般合同审查起草（本窗搁置）；

本任务不做行业监管申报正文；环境许可专项转环境能源，不给成交率或胜诉率。

## 建立评估坐标

确认：使用者与授权；单方立场；路径是股权还是资产；许可清单；许可对收入的依赖度；法域；当前磋商、文本、审批或交割状态；关键材料及缺失。

同时掌握买卖双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 陈述；`D#` 许可证书／年报；`L#` 已核验行业规则；`I#` 判断；`G#` 缺口；`P#` 承继假设；`V#` 版本

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验该许可是否可承继、是否需重新申请或变更；不虚构审批系统、案号或登记回执。

工具不可用时继续许可清单和股权／资产路径事实整理；依赖未核验规则的法律结论、期限或机关路径字段停止。

## 五个判断节点

执行顺序和停止条件以 [评估方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　分流和路径

环境能源专项转出。行业申报正文不实施。股权路径不等于许可自动稳定，资产路径不等于全部重办。

### 节点 2　许可清单

特许、资质、备案、年报、人员资质分列。过期或超范围标红旗，转许可尽调原子接口。

### 节点 3　承继与变更

主体变更、实际控制人变更、地址或场所变更是否触发重新申请待核。未核验不得写“股权收购故不用动证”。

### 节点 4　先批后交割

交易依赖该许可持续有效则标交割条件。未获批不得写成可交割营业。

### 节点 5　缺口和下一步

按关键许可不明、规则未核验、人员资质绑定排序。

## 停止与安全

- `整体停止`：立场冲突；指导无证经营或伪造许可。
- `字段停止`：行业规则、变更登记、人员绑定未核验。
- `目标停止`：主目标变为代办许可或环境专项。

## 交付

严格按 [输出合同](references/output-contract.md) 十节交付。正触发状态只用 `已形成、部分形成并明确受限字段、因停止条件未形成`。边界见 [路由](references/routing.md)。
