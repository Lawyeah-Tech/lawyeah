---
name: 执行医疗损害生效文书
description: 准备医疗损害生效文书的执行申请、金额和财产线索
---

# 执行医疗损害生效文书

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。不得把已失效《侵权责任法》医疗专章同号旧义当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。


## 任务目标

在明确、无利益冲突的权利人立场下，形成“执行申请、金额、财产线索”。

`depends-on` 为空。接受用户、律师、医学专家、机关或其他来源的等价事实、病历、鉴定、许可和费用清单。

## 工作边界

正触发：持有医疗损害生效判决、调解书或确认的调解协议未获履行，需要申请执行。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 文书未生效，实际要上诉；
- 要重审过错或鉴定；
- 只要财产保全；
- 单独私了协议尚未补强执行力。

本任务不重审过错或鉴定，不把无执行力私了协议当作执行依据，不给胜诉率，不指导伪造、抢夺、威胁、隐匿病历或中断救治取证。

## 建立坐标

确认：使用者与授权；单方立场；生效文书、履行期、已履行部分、财产线索、被执行人主体；法域；当前投诉、封存、鉴定、调解、监管或诉讼状态；机关文书作出与送达；对方已知主张；关键材料及缺失。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 材料表面；`A#` 当前一致／承认；`R#` 机关、鉴定或生效文书；`M#` 病历、实物或许可台账；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`Q#` 请求／抗辩／合规项；`V#` 评估或稿件版本；`E#` 封存／送达／提交凭证。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验民事诉讼法执行程序、生效法律文书执行依据现行条款；`search_practiced` 补路径与反例；特殊事实用 `search_case_by_fact`。不虚构鉴定、许可或案件系统。

工具不可用时继续事实、主体、材料和问题清单；执行期间和依据法律结论字段停止。

## 五个判断节点

执行顺序和停止条件以 [方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　依据

必须是有执行力的生效文书。
### 节点 2　期间

申请执行期间名称待核，不写唯一截止日。
### 节点 3　金额

按主文列项，已履行扣除，未知不写零。
### 节点 4　线索与主体

被执行人用文书记载主体；线索合法。
### 节点 5　草稿

不代提交，不重审实体。

## 停止与安全

- `整体停止`：立场不明；伪造生效文书或违法调查财产。
- `字段停止`：执行期间或依据规则未核验。
- `目标停止`：主目标变为上诉、重审或执行力补强。

## 交付

严格按 [输出合同](references/output-contract.md) 交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
