---
name: 应对竞争法民事诉讼
description: 形成竞争法民事案件首次应诉答辩包
---

# 应对竞争法民事诉讼

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 任务目标

在明确、无利益冲突的被告立场下，形成首次应诉答辩包：答辩期限卡、逐项抗辩、管辖异议接口、反诉评估接口、证据目录和答辩草稿。

`depends-on` 为空。

## 工作边界

正触发：已收到垄断或不正当竞争起诉状或应诉通知，本次主成果是首次答辩。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 用户实为原告只要起诉；
- 劳动应诉；
- 只要管辖异议终稿且明确不要实体答辩；
- 只要行政调查答复。

本任务不把应诉改写成主动起诉，不保证驳回。

## 建立应诉坐标

确认：被告；原告主张；送达日；举证期；是否已提管辖异议；是否存在仲裁协议；行政调查是否并行。

口头“对方说已起诉”不写成已送达。

## 来源和判断状态

编号：`S#` `D#` `A#` `R#` `C#` `L#` `I#` `G#` `Q#` `DEF#` 抗辩；`V#` `E#`。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。

## 使用 Lawyeah MCP

`search_law` 核验答辩期、管辖异议期、垄断民事解释抗辩。工具不可用时继续逐项事实抗辩；期限和管辖选定字段停止。

## 五个判断节点

以 [应诉方法](references/method.md) 为准。

### 1. 送达与期限卡

答辩期、管辖异议期分列。日期不明则期限字段停止。

### 2. 逐项抗辩

对原告每个请求独立回应：主体、市场、行为、正当理由、损害、时效。

### 3. 管辖与仲裁接口

只评估是否提出，不在本任务输出无实体的纯异议终稿，除非用户只要异议且负触发分流。

### 4. 反诉评估

只判断是否值得反诉，不自动生成反诉状终稿。

### 5. 答辩草稿

锁定 `V#`。不自认未支持事实。

## 停止与安全

- `整体停止`：立场冲突；伪造。
- `字段停止`：送达日、异议期未核验。
- `目标停止`：主目标变为主动起诉或调查答复。

## 交付

严格按 [输出合同](references/output-contract.md) 十节交付。边界见 [路由](references/routing.md)。
