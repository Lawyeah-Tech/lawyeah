---
name: 应对反垄断现场检查
description: 形成反垄断现场检查的到场权利义务和陈述边界卡
---

# 应对反垄断现场检查

## 任务目标

在明确、无利益冲突的被检查人立场下，形成“现场检查应对卡”：到场权利义务、封存配合、陈述边界、律师在场。

`depends-on` 为空。接受用户提供的到场通知、证件记录和被要求提供的范围。

## 工作边界

正触发：执法人员已到场或即将到场。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 指导藏匿、销毁或转移材料（整体停止）；
- 后续书面调查应对终稿（转 P0 应对反垄断调查）；
- 尚未被查的预案手册（转 准备现场检查应急手册）；

本任务不指导藏匿销毁，不替代后续调查应对。

## 建立评估坐标

确认：到场机关与证件；检查通知；场所；律师能否及时到场；已封存范围。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` `D#` `A#` `R#` `C#` `L#` `I#` `G#` `Q#` `V#` `E#`。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《合同法》及其解释当作现行规则。不得把 2008 年申报门槛或 2007 年反垄断法条号当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 使用 Lawyeah MCP

`search_law` 核验反垄断法调查措施、配合义务和妨碍调查处罚。工具不可用时继续到场纪律；合法拒绝范围字段停止。

工具不可用时继续事实、主体、程序文书状态和问题清单；法律结论及期限字段停止。

## 五个判断节点

执行顺序和停止条件以 [到场方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　证件与权限

出示证件、通知书。无法核验身份则记录并谨慎配合，不暴力拒绝。

### 节点 2　配合与封存

账簿、电子数据依法提供。封存清单核对。

### 节点 3　陈述边界

问什么答什么。不知道就说不知道。不猜测法律定性。

### 节点 4　律师在场

依法聘请律师。不编造一律有权阻止检查。

### 节点 5　禁止项

不得藏匿销毁。后续书面应对另项。

## 停止与安全

- `整体停止`：立场或授权不明；利益冲突未解；基础主体或做法无法建立；用户坚持伪造、隐匿、威胁、违法取证或指导规避执法。
- `字段停止`：法域、日期、文书送达、申报标准、动态规则未核验。
- `目标停止`：主目标已变为相邻原子或相邻领域的终稿。

## 交付

严格按 [输出合同](references/output-contract.md) 交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
