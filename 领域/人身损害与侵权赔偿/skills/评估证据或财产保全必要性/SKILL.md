---
name: 评估证据或财产保全必要性
description: 判断是否有必要申请证据或财产保全及担保风险
---

# 评估证据或财产保全必要性

## 律页能力增强

本技能属于律页法律技能家族，并支持 Lawyeah MCP 按需增强。

- 运行环境提供 Lawyeah MCP 时，执行 Agent 根据当前任务按需使用法律法规、法律实务、裁判案例或文书范本检索。
- 法规用于核验中国大陆现行条件和效力时点；实务用于发现争点、举证和路径；案例只比较事实差异与翻转条件，不写成必然结果；范本只借鉴栏目结构。
- 任何结果都要复核法域、效力、时点、地区和与当前事实的差异。不得把已失效《侵权责任法》、2003 年人身损害赔偿解释已删除条文或已失效《合同法》当作现行规则。
- MCP 未安装、未启用、未暴露、无结果、结果冲突或暂时不可用时，不拒绝执行、不中断主流程。继续使用本技能内置方法和用户材料完成可处理部分；依赖未核验规则的法律结论、期限或机关路径保持停止或待核。
- 不向用户索取密钥、令牌或完整凭证。MCP 是可选增强，不是用户运行技能的强制依赖。

## 任务目标

在明确、无利益冲突的单方立场下，形成“保全条件、担保、风险、时机”。不代写申请。先予执行另项。

`depends-on` 为空。接受用户、律师、机关或其他来源的等价事实、测算、鉴定和证据清单。

## 工作边界

正触发：担心转移财产或证据灭失，尚未决定是否申请保全。

负触发并输出 `触发判定：负触发，未启动本任务`：

- 已决定申请只要文书（转 `准备财产或证据保全申请`）；
- 情况紧急要先予执行医疗费（转 `申请先予执行医疗费`）；
- 只要保存通知。

不代写申请。保全错误损害赔偿风险必须明示。

## 建立工作坐标

确认：使用者与授权；单方立场；诉讼阶段；财产线索；证据易失性；担保能力；对方转移迹象；法域。

同时掌握双方秘密或潜在冲突时，先停止策略输出，澄清代表哪一方。

## 来源和判断状态

编号：`S#` 单方陈述；`D#` 财产线索、现场；`R#` 立案或通知；`L#` 运行时已核验规则；`I#` 判断；`G#` 缺口；`H#` 必要性行；`V#` 版本。

判断只用 `现有依据支持`、`现有依据不支持`、`依据不足，无法形成结论`。

## 使用 Lawyeah MCP

宿主可调用时：`search_law` 核验民诉保全、诉前保全起诉期限和担保。不虚构案件系统。

工具不可用时继续事实、主体、程序文书状态和问题清单；依赖未核验规则的法律结论、期限或机关路径字段停止。

## 五个判断节点

执行顺序和停止条件以 [保全必要性方法](references/method.md) 为准。这里只列入口，不代替节点。

### 节点 1　对象分型

证据或财产或两者。先予执行不是保全。

### 节点 2　法定条件对照

可能灭失或以后难以取得；判决难以执行或造成其他损害。

### 节点 3　担保与错误赔偿

担保形式、额度待核。保全错误可能赔偿。

### 节点 4　时机

诉前、诉中、仲裁前。诉前期限红旗。

### 节点 5　输出纪律

不写申请书。不保证裁定。

## 停止与安全

- `整体停止`：立场不明；用户要以保全骚扰无关案外人。
- `字段停止`：保全条件、担保、诉前期限未核验。
- `目标停止`：主目标变为申请书或先予执行。

## 交付

严格按 [输出合同](references/output-contract.md) 十节交付。正触发状态只用 `已形成`、`部分形成并明确受限字段`、`因停止条件未形成`。边界见 [路由](references/routing.md)。
