interface:
  display_name: "连带责任保证合同·审查"
  short_description: "审查连带责任保证待签文本。标题、鉴于和方式须同时出现连带责任保证，约定不明仍按一般保证，期间不明按六个月，主债必须特定。"
  default_prompt: "使用 $连带责任保证合同·审查，在单一立场下审查中国大陆待签连带责任保证合同。必须写明连带责任保证；约定不明按一般保证，不得用原担保法默认连带；运行时核验保证期间现行规则；主债权必须特定。独立保函、安慰函、一般保证文本负触发。给出可回给对方的修改句，不要保证有效，不要写争议策略。"
