interface:
  display_name: "连带责任保证合同·起草"
  short_description: "按已谈妥条件起草连带责任保证合同。标题、鉴于和方式同时写连带责任保证，触发用不履行并可直接请求，主债特定、期间写清；出稿后按上述硬闸自审。"
  default_prompt: "使用 $连带责任保证合同·起草，按已谈妥条件起草中国大陆连带责任保证合同。必须写明连带责任保证；约定不明按一般保证，不得用原担保法默认连带；运行时核验保证期间现行规则；主债权必须特定。独立保函、安慰函、一般保证文本负触发。给出可回给对方的修改句，不要保证有效，不要写争议策略。"
